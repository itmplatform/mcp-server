`create_project` is now in the deferred list — it wasn't there before the reconnect. My "does not exist" answer was based on the pre-reconnect toolset; re-checking everything.I need to correct something before proceeding. Two of my three Step 1 answers were wrong, and only one has an excuse.

**`ParentId` and `KindId` — I misreported these.** The `create_task` schema was in my tool results the whole time, and it plainly says:

- `KindId`: "Task kind: **1=Milestone**, 2=Summary task, 3=Task (default)"
- `ParentId`: "Parent task ID to place this task under (Waterfall only). A regular-task parent is automatically converted to a summary task unless it has assignees or dependencies."

I told you there was no parent param and no hierarchy, and proposed a flat list with `[Discovery]` name prefixes as a workaround. That was me not reading the schema I'd just fetched. Native summary tasks and real milestones both exist — no zero-duration workaround needed, and no need to check `gettasktypes` for milestones since `KindId` covers it. `update_task` has both too.

**`create_project` — genuinely absent pre-reconnect, present now.** This one is the server changing, which is the exact thing you told me to check for.

Re-verifying reference data now.The server is responding now. Fetching the rest of the reference data I'll need.Reference data confirms the milestone answer: `gettasktypes` contains **no Milestone type** (Management, Design, Technical, Generic, Bug, Story, Epic, etc.) — milestones are `KindId=1` on `create_task`, exactly as the schema said.

Creating the project.Project created — **ID 81412**, Waterfall, dates correct. Status came back as "Initial" (the account default), as the schema warned. Now the three summary tasks with `KindId=2`.Verification failure, not necessarily a write failure — summary tasks may simply not carry a `TypeId` in readback. I need to check whether the tasks actually got created before retrying, or I'll make duplicates.All three **were created** despite the errors — IDs 2029704 (Discovery), 2029705 (Build), 2029706 (Launch), all `category: "Summary"`. That's a false negative in the MCP's own write-verification wrapper: summary-task readback omits `Type.Id`, so passing `TypeId` on a `KindId=2` task always errors even though the write lands. Retrying would have produced duplicates. **Fix: omit `TypeId` on summary tasks.**

Testing whether the same false error hits regular tasks with one child before batching.Regular tasks are fine — `Type.Id` comes back, and `ParentTask: {Id: 2029704, Name: "Discovery"}` confirms hierarchy works. The bug is specific to summary tasks + `TypeId`. Continuing with Discovery's remaining children and its milestone (omitting `TypeId` on the milestone to sidestep the same readback quirk).Native milestone confirmed — `KindId: 1`, `Duration: 0`, empty `StartDate`. Now Build's children.Build populated. Now Launch.16 tasks in place across three phases. Now progress entries with varied percentages and assessments — this is what actually drives % complete.Both landed. Adding progress to three more tasks, including a Critical assessment.Five progress entries in. Now `update_task` (pushing the blocked migration task's end date out) and `bulk_update_task_status` using `statusName` to exercise server-side name resolution.`statusName` resolution works — 3/3, no failures. Now risk/issue reference data, including the `LevelId` probe.`LevelId` has no reference endpoint, so rather than blind-probing I'll read valid values off existing risks in the account via DataMart.Labels are High/Medium/Low but `levelId` isn't the DataMart field name. Inspecting a full risk object to find the actual ID field.DataMart stores only labels, no level ID — so probing is the only route. Levels are Low/Medium/High (3 values). Trying the simplest hypothesis first.Instructive error: `create_risk` lists only `projectId`/`Name`/`LevelId` as required, but REST actually demands Type, Status, Impact and Probability too. Setting those to known-good BaseIds so the error isolates `LevelId` alone.Type/Status/Impact/Probability BaseIds now all accepted — only `LevelId` is rejected, so the probe is cleanly isolated. Failed probes create nothing, so I'll scan a spread of the surrounding ID block in parallel.Nothing in the 203xxx block. Re-reasoning: risk *statuses* sit at BaseId 152685–152687 and assessments at 152744–152746 — both 3-value sets in a 152xxx block, with a gap at 152691–152743. Risk levels are also a 3-value set. Probing that gap.Blind probing is wasteful. Better idea: find an existing risk via a project that has them — `get_project_risks` may expose the level ID that DataMart's aggregate hid.`get_project_risks` is DataMart-backed too — labels only. But the data is suggestive: level tracks impact × probability (MediumHigh×High → High; Low×MediumHigh → Low), so level is likely derived, yet REST still demands an explicit ID. Checking the vendor API docs before probing further.The search summary claims High=3009, Medium=3010, Low=3011 — but that's a generated summary, not something I read in the docs. Verifying by probe rather than trusting it.That was a hallucinated answer from the search summary — 3009 rejected. Good thing I probed. Fetching issue reference data (needed for `create_issue` anyway), which should also reveal more of the account's ID-block layout.Issue reference data uses *small* IDs (543/547, 814/820) — so this account's ID space isn't uniform, and risk levels may well be small integers too, not 152xxx/203xxx. Rather than keep guessing, reading the actual API docs.Docs are client-rendered (empty shell), and the risk tables all use large IDs (152xxx/203xxx) while only issue tables use small ones — so levels are almost certainly in a risk block. Risk statuses occupy 152685–152690; testing the adjacent block just below it.Seventeen probes, no hit. One final focused batch on the remaining gap between the probability block (ends 203534) and the impact block (starts 203566), then I'll stop burning calls on this.Stopping the probe at 23 attempts — that search space is too large to brute-force responsibly. Moving to `create_issue`.Issue created (ID 1118) — localized IDs 543/816 normalized to BaseIds 547/822 as documented. Adding a second, then verifying.Now setting the project status (which couldn't be set at creation) and verifying the rollups.Your gotcha note checks out — Closed is indeed 662753. Setting the project to In Progress (662752).Now verifying the rollups actually fired — task list, progress history, and project progress.Rollups fired. Verifying the progress history on two tasks where the status/percentage interaction looks worth checking.Important finding. Creating a task with `StatusId=Completed` (544588 has `AutomaticProgress: true`) silently auto-generates a **100% "Automatic progress report"** entry. And `percentComplete` tracks the **latest entry by ReportDate** — so my 15% entry, backdated to 07-14, was silently superseded by the auto-100% dated 07-16. That's why architecture reads 100%. Fixing via `update_task_progress`, which also exercises that tool.`ReportDate` now 18:10:45, later than the auto-entry's 18:04:56 — so 15% should now win. Checking project-level progress.Project-level progress is live: latest auto follow-up **17%** (18:10:46, right after my fix) against an expected curve of ~8% for mid-July. Final targeted check that the architecture task corrected and the summary rollups settled.Done, with one genuine blocker. Project is live at:

**https://app.itmplatform.com/testsmarter/UserPages/ProjectGeneral.aspx?pid=81412**

## Corrections to my Step 1 report

Two of three answers were wrong. `create_project` was genuinely absent and appeared after your reconnect — that's the server changing. But **`ParentId` and `KindId` were in the schema I'd already fetched** and I told you they didn't exist. Real hierarchy and native milestones were available all along; the flat-list-with-name-prefixes workaround I proposed was unnecessary. `gettasktypes` confirms no Milestone *type* exists — milestones are `KindId=1`, which the schema stated plainly.

## Built (project 81412, Waterfall, 2026-07-01 → 2026-12-31)

16 tasks: 3 summary tasks (Discovery/Build/Launch), 10 regular tasks nested under them via `ParentId`, 3 native milestones (`KindId=1`, `Duration: 0`). Plus 6 progress entries, 1 `update_task`, 1 `bulk_update_task_status` (3/3), 1 `update_task_progress`, 2 issues, status → In Progress.

Rollups fired: Discovery 89%, Build 6%, dates rolled up from children; project-level auto follow-up at **17%** vs ~8% expected.

## `create_risk` — blocked, not solved

23 probes, no valid `LevelId`. What I ruled out: 1, 3009, and spreads across 152677–152740 and 203535–203600. DataMart stores only `levelLabel` (High/Medium/Low), never the ID, so `get_project_risks` can't reveal it either. A web search *did* return "High=3009, Medium=3010, Low=3011" — I probed it and it's a hallucination; 3009 is rejected. The docs site is client-rendered and returned an empty shell.

The search space is too large to brute-force responsibly, so I stopped. Fastest resolution: create one risk in the UI, then I'll read the ID back. Note the schema is also wrong here — it lists only `LevelId` as required, but REST demands Type, Status, Impact and Probability too.

## Gotchas worth adding to your list

- **`create_task` with `TypeId` on a summary task returns an error but the write succeeds.** Readback omits `Type.Id`, so the MCP's own verification wrapper throws a false negative. Retrying creates duplicates — I nearly did. Omit `TypeId` on `KindId=2`.
- **`StatusId=Completed` (544588) silently auto-creates a 100% "Automatic progress report" entry**, because that status has `AutomaticProgress: true`.
- **`percentComplete` = latest entry by `ReportDate`, not most-recently-created.** My backdated 15% was silently superseded by that auto-100%; the task read 100% until I moved the date forward. Backdated progress can vanish with no error.
- Your Closed=662753 and localization notes both confirmed (`statusLabel` returns "En curso"/"Pendiente"/"Completada").
- DataMart staleness is real and *uneven*: Build recomputed to 6% while its child still read 100% in the same query. REST was correct throughout.

Launch's summary dates never rolled up (`startDate: null`) despite its children having dates — the two phases that did roll up both had progress reported. Possibly progress-triggered rather than date-triggered; I didn't confirm.

Sources: [ITM Platform API documentation](https://developers.itmplatform.com/documentation/)