Complete reference for all MCP tools available to the AI. Each tool includes its parameter schema (generated from the server's actual tool definitions), an editorial narrative explaining what the AI does with it, and an example response.

Tool names, descriptions, and parameters are extracted from the MCP server at build time. The data below always matches the server version shown in the header.
